services:
  - type: web
    name: sniplink-api
    runtime: node
    buildCommand: npm run build --workspace=sniplink-api
    startCommand: node apps/api/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
